import React from 'react';
import styles from './Bottom.module.css';

const BottomNav = (props) => (
  <nav className={styles.container}>
    <p className={styles.comment}>Code your dream!</p>
  </nav>
);

export default BottomNav;
